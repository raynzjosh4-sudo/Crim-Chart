diff a/src/channel/ChannelComponents/ChnnelMainPostCard/postCardFiles/PostContent.tsx b/src/channel/ChannelComponents/ChnnelMainPostCard/postCardFiles/PostContent.tsx	(rejected hunks)
@@ -1,11 +1,17 @@
 import React from 'react';
-import { View, Text, StyleSheet } from 'react-native';
+import { View, Text } from 'react-native';
+import { useStyles } from '@/core/hooks/useStyles';
+import { ThemeTokens } from '@/core/theme/themes';
 
 interface PostContentProps {
   content: string;
 }
 
 export const PostContent: React.FC<PostContentProps> = ({ content }) => {
+  const styles = useStyles(themeStyles);
+
+  if (!content) return null;
+
   return (
     <View style={styles.container}>
       <Text style={styles.text}>{content}</Text>
@@ -13,14 +19,14 @@ export const PostContent: React.FC<PostContentProps> = ({ content }) => {
   );
 };
 
-const styles = StyleSheet.create({
+const themeStyles = (colors: ThemeTokens, scale: number) => ({
   container: {
-    paddingHorizontal: 16,
-    marginBottom: 12,
+    paddingHorizontal: 16 * scale,
+    marginBottom: 12 * scale,
   },
   text: {
-    color: '#FFF',
-    fontSize: 14,
-    lineHeight: 20,
+    color: colors.text,
+    fontSize: 14 * scale,
+    lineHeight: 20 * scale,
   },
 });
